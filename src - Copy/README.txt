Place DashboardHome.jsx in your project's src/ folder.
Place siteVersion.js in your project's src/ folder.
This update replaces the Deploy Center copy button with one single PowerShell release command that commits dev, pushes dev, merges into main, and pushes main.
